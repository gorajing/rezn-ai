# ── OS / editors ──────────────────────────────────────────────────────────
.DS_Store
*.pem

# Local editor / AI-tool configs (not project source)
.rocketride/
.claude/
.cursor/
.codex/

# ── Secrets ─────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.local.example

# ── Python ──────────────────────────────────────────────────────────────────
.venv/
__pycache__/
.pytest_cache/
*.pyc
*.egg-info/
build/
dist/

# ── Node / Next.js ───────────────────────────────────────────────────────────
node_modules/
/.pnp
.pnp.*
/.next/
/out/
/coverage
*.tsbuildinfo
next-env.d.ts
.vercel
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.openapi.tmp.json

# ── Generated run artifacts (large WAVs); keep the folder, not its contents ──
runs/*
!runs/.gitkeep
artifacts/runs/
artifacts/batches/

# Playwright MCP browser-automation logs (not source)
.playwright-mcp/

# ── Accidental nested clones of this repo (root-anchored; never commit) ──────
/rezn-ai/
/rezn-ai-1/
